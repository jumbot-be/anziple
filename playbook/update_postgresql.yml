---
- name: Update PostgreSQL
  hosts: all
  vars_files:
    - ../group_vars/all.yml
    - ../group_vars/all_secrets.yml
    - "../{{ include_release_vars | default('vars/releases/4.0.9.yml') }}"

  tasks:
    - name: Run PostgreSQL update tasks
      ansible.builtin.include_role:
        name: postgresql
        tasks_from: update

    - name: Cleanup temporary files
      ansible.builtin.include_role:
        name: cleanup
