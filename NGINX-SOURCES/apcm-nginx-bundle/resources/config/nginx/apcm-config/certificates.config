  ssl_certificate @nginx.home.directory@/certs/apcm-cert.crt;
  ssl_certificate_key @nginx.home.directory@/certs/apcm-cert.key;
  #ssl_trusted_certificate @nginx.home.directory@/certs/apcm-chain.pem;
  ssl_stapling on;
  ssl_stapling_verify on;