#define the list of ip that can access admin restricted path
#this includes keycloak administration and mui (if nginx.mui.access is set to admin)

allow 109.135.43.185; # Ampacimon public IP
allow 10.11.0.0/16; # Ampacimon internal network
allow 127.0.0.1; # local host
allow ::1; # local host

